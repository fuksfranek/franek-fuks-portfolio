/**
 * Delay before mounting #archive overlay after `shell--archive-exit` is applied.
 * Must match `.shell--archive-exit` opacity + filter duration in App.css (currently 0.22s).
 */
export const ARCHIVE_SHELL_EXIT_MS = 220
